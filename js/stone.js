class stone{
    constructor(x,y)
    this.height = height;
    this.width = width; 
{
    var options={
        isStatic:false,
        restitution:0,
        friction:1, 
        density:1.2
    }
    this.image=loadImage("images/stone.png")
}
}